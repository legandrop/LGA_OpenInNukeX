v1.78: Ventana de config alineada con el resto de las apps LGA y redimensionable en Y. Los titulos de seccion pasan al violeta `#775ECF` y los cards a 8 px de radio, que son los tokens `titulo_seccion` y `radio_card` que PipeSync ya tiene centralizados; la separacion entre bloques baja de 24 a 15, el valor del settingstab de PipeSync y FileManagerS3. Con eso los tres bloques se leen como una sola lista de opciones y no como tres pantallas pegadas. La ventana deja de ser de tamano fijo: se puede estirar en Y (en X no, porque el contenido es una columna de ancho fijo y ensanchar solo agrega fondo a los costados), con tope en el 80% del alto USABLE del monitor y piso en 400, y el QScrollArea que ya envolvia todo se hace cargo de lo que no entra. El tope no es solo un clamp del calculo automatico: es el `maximumHeight` real de la ventana, asi que arrastrar el borde tampoco la lleva mas alla. Y desde el momento en que el usuario la dimensiona a mano, la app no vuelve a imponerle un alto — desplegar las instrucciones manuales agrega contenido y desplaza hasta el en vez de crecer por debajo de sus manos. En File Association el boton APPLY baja del titulo al texto, centrado verticalmente junto a el: alineado con el titulo quedaba a dos renglones de lo que explica que hace, con un hueco vacio en el medio. [ OpenInNukeX - Titulos violeta, cards mas redondeadas y ventana redimensionable en Y ]

v1.78: Panel de instalacion manual del bridge, tres correcciones de lectura. El "Instalar manualmente…" era `class="link"` —azul, sin caja y sin borde—, o sea que tenia la pinta de un enlace que lleva a otro lado cuando en realidad despliega contenido en esta misma ventana; pasa a verse como el boton que es. La linea a pegar en el `init.py` estaba a la misma distancia de todo y sin sangria, leyendose como un item suelto y no como parte del paso 3: los tres pasos dejan de ser un unico `<ol>` en un QLabel y pasan a una grilla de numero + texto, que es lo que permite meter la caja de codigo en la columna del texto y por lo tanto exactamente bajo el paso que la nombra. Su texto ademas deja de ser violeta, que es el color de los titulos de seccion y de los botones de accion y aca sugeria una jerarquia que esa linea no tiene. Y COPY LINE se va de la fila de botones de abajo —donde no se sabia cual de las dos cosas copiaba— a la derecha de la linea que copia. [ OpenInNukeX - Panel manual del bridge: boton visible, codigo dentro del paso 3 y copiar al lado de la linea ]

v1.78: El deploy de Windows estaba roto: `instalador.bat` cortaba con `ERROR: Falta resources\SetUserFTA.exe` teniendo el archivo al lado. La causa es el parseo de argumentos que se reescribio en la v1.77: sus `shift` iban sin `/1`, y `shift` a secas desplaza tambien `%0`, asi que despues de consumir la primera opcion `%~dp0` dejaba de ser la carpeta del script y pasaba a ser la del que llamo. Con eso `QTCLIENT_DIR` apuntaba a otro repo y todo chequeo de ruta fallaba. Afectaba a `deploy.bat` —que recibe `--no-run` siempre— y a `compilar_dev.bat`, que recibe `--release` siempre, o sea las dos rutas de compilacion de Windows. Los `shift` pasan a `/1`. [OpenInNukeX - Corregir el shift que rompia las rutas del deploy de Windows]

v1.78: El `install.pdf` se reemplaza por `install_es.pdf` e `install_en.pdf`. La hoja vieja salia de exportar un Google Doc a mano y quedo congelada en el metodo manual: no menciona los instaladores que el ZIP trae desde hace varias versiones, y ademas daba el backup del `init.py` como `init.py.bak`, un nombre que los motores ya no usan: hoy guardan una copia numerada en `~/.nuke/LGA_init_backups/`. Las dos hojas se generan ahora desde una plantilla unica en el repo de release, con la version leida del `VERSION` del repo, asi que el texto comun deja de mantenerse por separado en cada producto. Se documenta tambien que en macOS el instalador va con `bash installer_mac.sh`: los `.sh` pierden el permiso de ejecucion dentro del `.zip` y `./installer_mac.sh` da `Permission denied`. [OpenInNukeX - Reemplazar install.pdf por las hojas en castellano e ingles]

v1.77: El build ya no se registra en el registro compartido de LGA. `LgaRegistry::registerThisApp()` escribia el `installPath` en CADA arranque, asi que el ultimo binario abierto ganaba, y en desarrollo ese es siempre el del arbol de compilacion: el card de LGA Updates de PipeSync terminaba mostrando `.../QtClient/build/LGA OpenInNukeX.app` en lugar de la copia instalada. Las salidas de desarrollo son lo UNICO que no registra; cualquier otra ubicacion si, sea `/Applications`, `Program Files` o una carpeta cualquiera del usuario. Es una lista negra a proposito: la app se puede instalar en cualquier lado y una lista blanca de ubicaciones "validas" dejaria sin registrar instalaciones legitimas. Se reconocen de dos formas complementarias. Por el NOMBRE de la carpeta que contiene la app (`build`, `deploy`, ...), mirando SOLO esa carpeta y nunca la ruta entera: `D:\Builds\Apps\X.exe` es una instalacion legitima y se registra, y mirar la ruta completa la voltearia en silencio, que es este mismo bug al reves; este chequeo no depende de que nadie cablee nada. Y por las rutas reales del proyecto que inyecta CMake —`LGA_BUILD_TREE_DIR` y `LGA_SOURCE_TREE_DIR`, este ultimo porque el `deploy/` es hermano del build y no hijo—, que agarran lo que el nombre no ve, como un arbol de build llamado `out/`; se comparan por componente para que `/x/build2` no cuente como dentro de `/x/build`, y en Windows son case-insensitive. Cuando se saltea queda la razon en el log. [OpenInNukeX - El build deja de pisar la ruta instalada en el registro compartido]

v1.77: El instalador del componente de Nuke que se agregaba a QtClient era un duplicado: los instaladores de este producto ya existen en el repo de release (`Installers/LGA_OpenInNukeX-Nuke/`) y forwardean a un motor comun transaccional, con backup, rollback, validacion del `init.py` y el ordenamiento de imports LGA que respeta las lineas indentadas dentro de un `if`. El escrito a mano no tenia nada de eso. Se elimina y `deploy.sh` toma el wrapper y el motor del deposito, cortando si no esta. Ademas se descubrio por que ese producto nunca pudo instalarse desde el card de PipeSync: era el unico de los siete cuyos instaladores no seguian la convencion de nombres, y el card busca `installer_mac.sh` / `i_win_engine.ps1` exactos. El rename va en el repo de release. El zip suma tambien el `install.pdf`, como el de Windows. [OpenInNukeX - Usar el instalador comun del repo de release en vez de uno propio]

v1.77: Correcciones salidas de la revision del Nuke Bridge, todas de casos que la implementacion inicial daba por cubiertos y no lo estaban. (1) El `installer_mac.sh` NO tenia el guard de repositorio fuente que si tiene el boton INSTALL: corrido apuntando a `~/.nuke` en una maquina de desarrollo —que es lo que hace el card de updates de PipeSync— sobrescribia `init.py`, `LGA_QtAdapter_OpenInNukeX.py` y `VERSION` versionados con la copia empaquetada. Se reprodujo de verdad durante la prueba y se recupero con git. (2) Una linea `pluginAddPath` COMENTADA contaba como instalada, tanto en la app como en el instalador: comentarla es el gesto natural para desactivar el bridge un rato, y con eso el chip decia "instalado" y el instalador informaba exito sin agregar nada, con Nuke sin cargar el plugin. Ahora se recorre linea por linea salteando las comentadas. (3) Sin `VERSION` legible el chip mostraba en verde la version que la app TRAERIA, no la que hay: un bridge viejo se veia al dia, que es justo lo que el chip existe para detectar. (4) `install()` creaba la carpeta destino antes de chequear que hubiera algo que copiar, dejando una `LGA_OpenInNukeX/` vacia en el plugin path de Nuke. (5) El hint afirmaba haber encontrado la carpeta con solo tener texto en el campo, y el path se insertaba sin escapar en un label RichText. (6) El chip no se refrescaba al editar la ruta a mano. (7) En Windows salian DOS carteles contradictorios: el de la falla y, acto seguido, el de "ya podes hacer doble click". [OpenInNukeX - Correcciones de la revision del Nuke Bridge]

v1.77: Empaquetado, correcciones de la revision: la validacion de binarios universal2 estaba anulada con `|| true`, siendo el unico chequeo de arquitectura del pipeline; `rm -rf deploy` hacia que `--zip` borrara el `.dmg` de la misma version y viceversa; el script terminaba con exit code 1 al contestar "N" a la pregunta final, o sea que un deploy exitoso se reportaba como fallido; `create_dmg.sh` usa `strings` sin declararlo como requisito, y sin el borraba un DMG bueno culpando al layout; y `github_release_mac.sh`, sin terminal y sin `--yes`, asumia que si a reusar un `deploy/` cuyo unico criterio de validez es que el NOMBRE coincida con la version — podia publicar un artefacto construido desde otro commit. `deploy.bat` ademas se llevaba al instalador de Windows el arbol `_autogen\` y demas intermedios de Ninja. Se agrega `--no-rosetta`: en Apple Silicon la app se lanza traducida por un workaround historico de Qt 6.5.3, con lo cual la rebanada arm64 no se ejecutaba nunca en desarrollo y `--sim-slow` terminaba midiendo Rosetta sobre E-cores. Y se corrige un bug latente de `tools/sync_version.py`: usaba `Path.write_text(newline=...)`, que existe recien en Python 3.10 mientras que macOS trae 3.9 — solo saltaba cuando habia algo que escribir de verdad, asi que nunca se habia visto. [OpenInNukeX - Correcciones de la revision del empaquetado]

v1.77: Nuke Bridge: la instalacion del componente de Nuke pasa a hacerse desde la propia ventana de config, en un tercer bloque con la carpeta `.nuke` detectada pero editable, estado (sin instalar / instalado / hay actualizacion) y una salida manual desplegable que exporta los `.py` y da la linea exacta para pegar en el `init.py`. El instalador nunca reescribe el `init.py` del usuario: solo appendea su linea y solo si falta, y se niega a instalar si el destino es el repositorio fuente —el repo vive justamente en `~/.nuke/LGA_OpenInNukeX/`, que es la carpeta destino, asi que instalar ahi pisaba los `.py` originales—. Al instalar bien, publica la carpeta `.nuke` en el registro compartido de LGA, que es de donde la lee el card de updates de PipeSync; hasta ahora esa mitad del contrato no tenia escritor. El payload viaja DENTRO del artefacto (`Contents/Resources/bridge` en mac, `bridge\` junto al `.exe` en Windows) y los dos deploys cortan si falta, porque sin el la app instala nada y eso recien se nota en la maquina del usuario. Se agrega ademas `installer_mac.sh`, el entry point que PipeSync ejecuta para instalar la herramienta desde su card; el release de macOS no lo tenia y por eso esa app nunca pudo instalarse desde ahi. Nueva capa bilingue EN/ES con switch al pie, junto a la version: el idioma lo elige el usuario y cambia en caliente, y queda persistido. Ver `docs/Doc_Nuke_Bridge.md`. [OpenInNukeX - Nuke Bridge instalable desde la app, con salida manual y capa bilingue]

v1.77: Se porta el componente `Dialogs` del template: todo cartel modal deja de ser un `QMessageBox` suelto y pasa a tener la marca violeta que indica siempre que hace Enter, navegacion con flechas, y paths coloreados en su propia linea. Los textos de error del motor del bridge se devuelven como CODIGO y no como string: el motor no sabe en que idioma esta la ventana, y con el texto adentro el cartel salia siempre en un solo idioma. Ademas la ventana deja de poder crecer mas alla de la pantalla: con el panel manual desplegado quedaba en 1024 px arrancando en y=224 sobre un escritorio de 1117, asi que el pie —el selector de idioma y la version— quedaba abajo del borde y, al ser la ventana de tamano fijo, no habia forma de llegar. Se corrige tambien el ancho, que estaba escrito 900 en el constructor y 800 en el resize, asi que la ventana cambiaba de ancho sola al terminar el escaneo. [OpenInNukeX - Portar Dialogs y arreglar el tamano de la ventana]

v1.77: Empaquetado macOS alineado con la convencion LGA. La app pasa a llamarse `LGA OpenInNukeX.app` y a vivir en `/Applications`: estaba exceptuada del rename porque vivia en `~/.nuke`, y el bridge es justamente lo que desacopla donde vive la app de donde Nuke busca sus plugins. `CFBundleName` va sin el prefijo (la barra de menu no lo necesita) y el bundle id deja de quedar vacio, porque la propiedad de CMake estaba escrita `MACOSX_BUNDLE_IDENTIFIER` en vez de `MACOSX_BUNDLE_GUI_IDENTIFIER` y CMake no se queja. Los cuatro scripts de compilacion pasan a tener dos arboles separados (`build/` Debug y `build-release/` Release) con deteccion del build type cacheado —sin eso pedir Release sobre un arbol en Debug compilaba Debug en silencio—, mas `--wait`, `--no-run` y `--sim-slow`; y dejan de retener la terminal, que era un cuelgue indefinido para cualquiera que compilara por CLI. `compilar.sh` y `compilar.bat` forwardeaban sin `--release`, o sea que el script "de release" compilaba Debug. El deploy firma ad-hoc, empaqueta con `ditto` y no con `zip` (que resuelve los symlinks y rompe la firma), verifica el artefacto ya armado y arma el DMG con la cadena vendorizada del template. `deploy.bat` deja de usar un tercer arbol propio con su copia duplicada de la logica de CMake. [OpenInNukeX - Rename a LGA OpenInNukeX y empaquetado macOS completo]

v1.76: Instalador del plugin Nuke: dejaba el `init.py` roto en cualquier equipo que tuviera sus `pluginAddPath` adentro de un bloque `if` —por ejemplo para discriminar por version de Nuke— y ademas reportaba exito. Al reordenar los paths se llevaba tambien las lineas indentadas, dejando el bloque sin cuerpo, y Nuke no arrancaba con un `IndentationError`. Ahora solo toca las lineas en columna 0 y respeta las indentadas donde estan. Suma ademas deduplicacion de paths repetidos, preservacion del BOM del archivo original y una validacion del resultado: si el `init.py` quedaria invalido, no lo modifica y aborta la instalacion. No afecta al instalador Qt/C++, que es independiente. [OpenInNukeX - Corregir el manejo del init.py del instalador]

v1.75: Distribucion: se incorporan instaladores transaccionales para el componente Python/Nuke en Windows y macOS, separados del setup Qt/C++. Los releases de cada plataforma los incluyen junto al payload `LGA_OpenInNukeX`; validan archivos obligatorios, respaldan la carpeta previa, configuran el `init.py` global de forma idempotente y restauran ante fallos. Tambien se corrigen las superficies visibles que habian quedado en v1.71 para alinearlas con la version publicada v1.73 y se versionan las instrucciones de trabajo faltantes. [OpenInNukeX - Agregar instaladores del plugin Nuke]

v1.74: Rules: se agregan `.codex/instructions.md` + `.cursor/rules/instructions.mdc` (ambos nuevos) alineados con el estandar unificado LGA. Estructura del repo es de dos partes (servidor Python en la raiz + cliente Qt en `QtClient/`), la app vive en `~/.nuke/LGA_OpenInNukeX/` por requisito de Nuke (NO paralelo al Base como las demas apps LGA), por eso las referencias al Base usan path absoluto documentado (`~/Desktop/Codin/LGA_Base_QT_C_Py` en mac, `C:/portable/LGA_Base_QT_C_Py` en win). Contenido: hub de aprendizajes Qt/C++ en el Base con reglas de leer/escribir (el bloque de purga de AGL del CMakeLists sale del hub); docs unificadas del Base como fuente de verdad; seccion Build con path a `QtClient/` (los scripts viven ahi, no en raiz); "No compilar por defecto"; politica de idioma (UI ingles + comentarios castellano); versionado append-only con sugerencia `[ ]` y confirmacion de que el repo YA usa el patron LGA canonico "CMake single source" (`OPENINNUKEX_VERSION`); Help/About tab convention; convencion GitHub con lista explicita de herramientas AI y ejemplos malo/bueno; nota "espejo" al inicio para mantener `.codex` y `.mdc` sincronizados. Parte de la unificacion cross-app con Base, PipeSync, FM, LinkRedirector y ThetaMacEplorer. [Rules - Unificar con estandar LGA (hub Qt/C + docs Base + politica idioma + AI)]

v1.73: Build macOS / AGL: arreglo de raiz del `ld: framework 'AGL' not found` y del mismatch de version que aparecia cuando el sistema exponia una stub construida para un macOS mas nuevo. El fallback `-framework AGL` lo agrega Qt6 via `WrapOpenGL::WrapOpenGL` (definido en `$QTDIR/lib/cmake/Qt6/FindWrapOpenGL.cmake`) por transitividad de `Qt6::Widgets`. En `QtClient/CMakeLists.txt` se agregan dos bloques: el preventivo `set(OPENGL_agl_LIBRARY "")` antes de `find_package(Qt6 ...)` y el purgador que quita AGL de `WrapOpenGL::WrapOpenGL` despues. En `QtClient/compilar.sh` y `QtClient/compilar_dev.sh` se elimina el dummy `AGL.framework` y el flag `-F $PWD`; `compilar_dev.sh` incluye migracion one-shot para limpiar el `-F` heredado en la cache. [Build macOS - Fix AGL sin dummy framework]

v1.72: macOS: nuevo icono con el estandar glass de la suite LGA — squircle plano al 80% de la grilla de Apple (Tahoe le agrega el rim de Liquid Glass), glyph al 74% con padding y fondo gris igual a PipeSync (gradiente 46 a 28, nunca negro). El glyph (circulo CMY + simbolo nuke) se toma de la fuente SVG limpia LGA_OpenInNukeX.svg por saturacion, no del icono horneado, para no arrastrar el borde viejo. Ademas compilar_dev.sh/compilar.sh refrescan el cache de iconos del bundle (touch + lsregister -f) tras compilar, para que el Dock/Finder no muestren el icono viejo. [Mac - Icono glass 80% + refresh de cache de iconos]

v1.71: En Windows, la barra de titulo nativa de `OpenInNukeX Config` se fuerza siempre a modo oscuro mediante DWM. Se activa `DWMWA_USE_IMMERSIVE_DARK_MODE` con compatibilidad para los IDs usados por distintas versiones de Windows y, cuando Windows 11 lo permite, se fijan explicitamente el fondo `#161616` y el texto claro de la caption, evitando depender del tema global del sistema o del estilo Qt. Ademas, `instalador.bat` regenera siempre el deploy Release antes de invocar Inno Setup, evitando que el instalador empaquete un ejecutable anterior al ultimo cambio. [UI Windows - Forzar barra oscura y actualizar deploy del instalador]

v1.70: El comando `paste_clipboard` conecta los nodos Read pegados a un `LGA_ContactSheet` y conecta el primer Viewer disponible al resultado. Se incorpora ademas el flujo moderno de mantenimiento del cliente Qt para Windows: compilacion incremental con Ninja, MinGW 13.1 y lld; despliegue automatico de dependencias y recursos; ejecucion al terminar; versionado centralizado; y reglas de trabajo del repositorio. [Build Qt - Modernizar compilacion y sincronizar version 1.70]

v1.69: Se agrega el comando TCP `paste_clipboard` para pegar nodos desde el clipboard en el proyecto activo de NukeX sin cerrar ni abrir scripts. [Servidor NukeX - Agregar paste clipboard]

v1.68: Se agrega logging detallado para diagnosticar la apertura de scripts y se deja la instrumentacion de mayor costo controlada por `DETAILED_OPEN_LOGGING`. [Logging - Diagnostico de apertura de scripts]

v1.66: Se consolida el cliente Qt multiplataforma, el instalador y la asociacion de archivos `.nk` sin requerir permisos administrativos durante la ejecucion de la aplicacion. [Qt Client - Release multiplataforma]
